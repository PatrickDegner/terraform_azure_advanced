# Local .terraform directories
**/.terraform/*

# other tf files
.terraform.lock.hcl

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data
*.tfvars

# Except for variables.tfvars files, which are safe to share and may be needed by other users
!variables.tfvars

# Ignore override files as they are usually used to override resources locally and so
# likely they contain local specific info
*_override.tf
*_override.tf.json

# .terraformrc file
# If you're on Windows, uncomment the next line:
# .terraformrc

# Ignore CLI config files, which are usually used to set sensitive data locally
.terraformrc
terraform.rc

# Ignore IDE files
.idea/
*.iml

# Ignore text editor files
*.swp
*.swo

# Ignore macOS files
.DS_Store

# Ignore plan files. They can contain sensitive info and should not be committed
*.tfplan